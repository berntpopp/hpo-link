# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
dist/
build/
.coverage
htmlcov/
coverage.xml
.pytest_cache/
.mypy_cache/
.ruff_cache/
# Env
.env
.env.local
# Data (built/downloaded + raw source artifacts — never commit)
data/*
!data/.gitkeep

# Local docker env holds production secrets — never commit it
# (keep .env.docker.example, which is a different, tracked filename)
.env.docker
